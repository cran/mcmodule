% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/mc_plot.R
\name{mcmodule_tornado}
\alias{mcmodule_tornado}
\title{Plot Tornado-Style Correlation Results Across Variates}
\usage{
mcmodule_tornado(
  mcmodule = NULL,
  corr_results = NULL,
  output = NULL,
  by_exp = FALSE,
  match_variates = TRUE,
  variates_as_nsv = FALSE,
  print_summary = TRUE,
  progress = FALSE,
  method = c("spearman", "kendall", "pearson"),
  use = "all.obs",
  lim = c(0.025, 0.975),
  colour = "strength"
)
}
\arguments{
\item{mcmodule}{(mcmodule object, optional). Module used to compute correlations
when \code{corr_results} is NULL.}

\item{corr_results}{(data frame, optional). Output table from \code{\link[=mcmodule_corr]{mcmodule_corr()}}.
If provided, no new correlation analysis is run. Default: NULL.}

\item{output}{(character, optional). Output node name. Passed to
\code{\link[=mcmodule_corr]{mcmodule_corr()}} when \code{corr_results} is NULL.}

\item{by_exp}{(logical). Passed to \code{\link[=mcmodule_corr]{mcmodule_corr()}}. Default: FALSE.}

\item{match_variates}{(logical). Passed to \code{\link[=mcmodule_corr]{mcmodule_corr()}}. Default: TRUE.}

\item{variates_as_nsv}{(logical). Passed to \code{\link[=mcmodule_corr]{mcmodule_corr()}}. Default: FALSE.}

\item{print_summary}{(logical). Passed to \code{\link[=mcmodule_corr]{mcmodule_corr()}}. Default: TRUE.}

\item{progress}{(logical). Passed to \code{\link[=mcmodule_corr]{mcmodule_corr()}}. Default: FALSE.}

\item{method}{(character). Passed to \code{\link[=mcmodule_corr]{mcmodule_corr()}}.
Default: \code{c("spearman", "kendall", "pearson")}.}

\item{use}{(character). Passed to \code{\link[=mcmodule_corr]{mcmodule_corr()}}. Default: "all.obs".}

\item{lim}{(numeric vector). Passed to \code{\link[=mcmodule_corr]{mcmodule_corr()}}.
Default: \code{c(0.025, 0.975)}.}

\item{colour}{(character or logical). Colouring for max absolute points.
Default: "strength". If \code{TRUE} or "strength", points are coloured by
qualitative correlation strength.}
}
\value{
A ggplot2 object.
}
\description{
\ifelse{html}{\href{https://lifecycle.r-lib.org/articles/stages.html#experimental}{\figure{lifecycle-experimental.svg}{options: alt='[Experimental]'}}}{\strong{[Experimental]}}

Creates a tornado-style plot from \code{\link[=mcmodule_corr]{mcmodule_corr()}} results. For each input node,
the plot shows all variate-level correlations as small vertical ticks, a black
horizontal range line (min to max), a median marker, and a larger marker at the
maximum absolute correlation.
}
\details{
\code{mcmodule_tornado()} returns a ggplot object. Use \code{\link[=mcmodule_corr]{mcmodule_corr()}} when you
need the correlation table; use \code{mcmodule_tornado()} when you need a plot
object that can be further customised.
\strong{Interpretation:} In the tornado plot each point (one per variate) shows the
correlation between that input and the chosen output across the model variates.
The coloured point highlights the variate with the maximum absolute correlation
for each input and is used to rank inputs. The black point is the median
correlation across variates and the black horizontal line shows the range
(minimum to maximum) of correlations for that input. The grey horizontal line
connects the maximum-absolute point to the zero-correlation vertical line to
facilitate interpretation. Use \code{mcmodule_corr()} to inspect the numeric
per-variate correlations, the plot is designed to give a compact visual
summary.
}
